## 1. Metadata

- [x] 1.1 Add reactive global head metadata for `theme-color` and supported color schemes.

## 2. Verification

- [x] 2.1 Verify with Playwright MCP that the document head updates `theme-color` when toggling dark and light mode.
- [x] 2.2 Run focused formatting/lint checks for the touched file.
