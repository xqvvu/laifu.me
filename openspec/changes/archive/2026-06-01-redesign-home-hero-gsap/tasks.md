## 1. Dependencies and Structure

- [x] 1.1 Add `gsap` as a project dependency if missing.
- [x] 1.2 Update the home hero markup in `app/pages/index.vue` for the new composition while preserving existing data queries and article sections.

## 2. Hero Visual and Motion

- [x] 2.1 Rebuild `app/components/HomeHeroVisual.vue` as the new editorial visual with SSR-rendered HTML/CSS.
- [x] 2.2 Add GSAP mounted animation with scoped selectors, cleanup on unmount, and reduced-motion behavior.
- [x] 2.3 Ensure responsive layout avoids overlap and remains readable on mobile and desktop.

## 3. Validation

- [x] 3.1 Run formatting/checks appropriate for the changed files.
- [x] 3.2 Verify the home page visually in a browser at desktop and mobile sizes.
- [x] 3.3 Validate the OpenSpec change.
