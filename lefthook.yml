pre-commit:
  files: git diff --name-only --cached
  parallel: true
  jobs:
    - name: format
      run: pnpm format:check

    - name: lint
      run: pnpm lint

pre-push:
  jobs:
    - name: build
      run: pnpm build
